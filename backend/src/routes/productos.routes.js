//aqui se crea la ruta para ser usada por express
const{Router} = require("express");


//se debe instanciar la ruta para ser usada
const router = Router(); 

//se debe exportar la ruta para ser usada 
module.exports = router; 